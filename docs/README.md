# 기능 목록

> **[목표]**
> 1. 리팩터링 해보기: 동작코드 작성 → 테스트코드 작성 → 테스트코드를 믿고 리팩터링
> 2. MVC 패턴으로 작성해보기
> 3. DDD로 작성해보기 

---

## 🧩기능 목록

### model

1. BridgeGame
    - [x] 생성한 다리 저장 → BridgeGame 생성자로 해결
    - [x] 게임 결과 반환 → result
    - [x] 게임 재시작 → retry

      재시도 회수 저장
    - [x] 다리 이동 → move
        
        위 다리와 아래 다리를 map으로 구분하여 저장
        - [x] 해당 다리로 이동할 수 있는지 확인 → compareMovableCell
        - [x] 해당 다리에 이동 결과 추가 → addProgress
    - [x] 계속 이동할 수 있는지 확인 → isContinue
    - [x] 다리를 건넜는지 떨어졌는지 확인 → isFallBridge
2. InputValidator
    - [x] 다리 길이 입력 값의 유효성 검사 → BridgeSize
    - [x] 다리 이동 입력 값의 유효성 검사 → Moving
    - [x] 게임 재시작/종료 여부 입력 값의 유효성 검사 → GameCommand

### view

1. InputView
    - [x] 다리 길이 입력 기능 → readBridgeSize
    - [x] 이동할 칸 입력 기능 → readMoving
    - [x] 게임 재시작 입력 기능 →readGameCommand
2. OutputView
    - [x] 게임 시작 출력 기능 → printStart
    - [x] 게임 중 현재까지 이동한 다리 출력 기능 → printMap
    - [x] 게임 종료 후 최종 결과 출력 기능 → printResult
    - [x] 게임 종료 후 성공 여부, 시도 횟수 등 게임 정보 출력 기능 → printStatus

### controller

1. GameController
    - [x] 게임 초기화 → initGame
        1. 다리 길이 리턴
        2. 다리 생성
        3. 게임 시작 호출
        4. 게임 종료 호출
    - [x] 게임 시작 → startGame
        1. 다리 이동 리턴
        2. 다리 이동
    - [x] 게임 재시작 → retryGame
      1. 재시작 여부 리턴
      2. R 입력 시 게임 시작 호출
    - [x] 게임 종료 → endGame
        1. 게임 종료 결과 출력
2.  InputController
    - **잘못된 값 입력시 에러 메시지 출력 후 재입력**
    - [x] 다리 길이 입력 및 검증 → inputBridgeSize
        1. 다리 길이 입력
        2. 다리 길이 유효성 검사
    - [x] 이동할 칸 입력 및 검증 → inputMoving
        1. 이동할 칸 입력
        2. 이동할 칸 유효성 검사
    - [x] 게임 재시작 입력 및 검증 → inputGameCommand
        1. 게임 재시작 입력
        2. 게임 재시작 유효성 검사

### util

1. ConvertData
    - [x] 다리 칸 숫자에서 문자로 변환 기능 → mappingBridgeCellIntToString
    - [x] 입력 받은 문자를 정수로 변환 기능 → convertStringIntoInt

### BridgeMaker

- [x] 다리 생성 → makeBridge

---

## 🚫예외 처리 목록

- [x] 다리 길이는 3~20 사이의 숫자만 입력 가능
- [x] 플레이어가 이동할 칸은 U와 D만 입력 가능
- [x] 게임 재시작/종료 여부는 R과 Q만 입력 가능
- [x] 예외 처리시 “[ERROR]”로 시작하는 에러 문구 출력
    - try catch문 사용시 `Exception` 은 사용 금지

---

## 🛠테스트 목록

### 다리 생성

1. 다리 생성 입력이 제대로 작동되는지
2. 다리가 제대로 생성되는지

### 플레이어 이동

1. 이동 입력이 제대로 작동되는지
2. 이동 후 출력이 올바르게 나오는지

### 재시작

1. 재시작 입력이 제대로 작동되는지
2. 재시작 시 기존 다리 적용 되는지
3. 재시작 시 시도 횟수 추가가 제대로 되는지

---

## ⌨입출력 예시

```
다리 건너기 게임을 시작합니다.

다리의 길이를 입력해주세요.
3

이동할 칸을 선택해주세요. (위: U, 아래: D)
U
[ O ]
[   ]

이동할 칸을 선택해주세요. (위: U, 아래: D)
U
[ O | X ]
[   |   ]

게임을 다시 시도할지 여부를 입력해주세요. (재시도: R, 종료: Q)
R
이동할 칸을 선택해주세요. (위: U, 아래: D)
U
[ O ]
[   ]

이동할 칸을 선택해주세요. (위: U, 아래: D)
D
[ O |   ]
[   | O ]

이동할 칸을 선택해주세요. (위: U, 아래: D)
D
[ O |   |   ]
[   | O | O ]

최종 게임 결과
[ O |   |   ]
[   | O | O ]

게임 성공 여부: 성공
총 시도한 횟수: 2
```

```
다리 건너기 게임을 시작합니다.

다리의 길이를 입력해주세요.
3

이동할 칸을 선택해주세요. (위: U, 아래: D)
U
[ O ]
[   ]

이동할 칸을 선택해주세요. (위: U, 아래: D)
U
[ O | X ]
[   |   ]

게임을 다시 시도할지 여부를 입력해주세요. (재시도: R, 종료: Q)
Q
최종 게임 결과
[ O | X ]
[   |   ]

게임 성공 여부: 실패
총 시도한 횟수: 1
```

---

## ✅ 기존 체크 리스트

- [x]  JDK 11 버전에서 실행 가능한가?
- [x]  Java 코드 컨벤션 가이드를 준수하였는가?
- [x]  indent depth가 2를 넘지 않았는가?
- [x]  3항 연산자를 사용하지 않았는가?
- [x]  테스트 코드를 작성해서 확인해보았는가?
- [x]  else 예약어와 switch/case를 사용하지 않았는가?
- [x]  도메인 로직에 단위 테스트를 구현했는가?

---

## ✅ 신규 체크 리스트

- [x]  메서드의 길이가 10라인을 넘지 않았는가?
- [x]  메서드의 파라미터 개수는 최대 3개까지만 사용하였는가?
- [x]  InputView
    - 해당 클래스에서만 readline() 사용 가능
    - 패키기 변경 가능
    - 메서드의 시그니처(인자, 이름)와 반환 타입 변경 가능
    - 메서드 추가 가능
- [x]  OutputView
    - 패키지 변경 가능
    - 메서드 이름 변경 불가, 인자와 반환 타임은 변경 가능
    - 메서드 추가 가능
- [x]  BridgeGame
    - 해당 클래스에서 InputView, OutputView를 사용 불가능
    - 필드(인스턴스 변수) 추가 가능
    - 패키지 변경 가능
    - 메서드 이름 변경 불가능, 인자와 반환 타입은 변경 가능
    - 메서드 추가 가능
- [x]  BridgeMaker
    - 필드(인스턴스 변수) 변경 불가능
    - 메서드 시그니처(인자, 이름)와 반환 타입 변경 불가능
- [x]  BraidgeRandomNumberGenerator
    - bridge.BridgeRandomNumberGenerator의 generate() 활용
    - BridgeRandomNumberGenerator, BridgeNumberGenerator 클래스의 코드 변경 불가능

---

## ✅ 개인 체크 리스트

- [x]  실행 결과 예시와 동일한 출력을 내는가?
- [x]  하드 코딩하진 않았는가?
- [x]  구현 순서를 잘 지켰는가?
- [x]  변수 이름에 자료형이 들어가지 않았는가?
- [x]  MVC패턴의 규칙을 지켰는가?
- [x]  final 키워드를 사용해 값의 변경을 막았는가?
- [x]  인스턴스 변수는 private으로 구현했는가?
- [x]  객체는 객체스럽게 사용했는가?